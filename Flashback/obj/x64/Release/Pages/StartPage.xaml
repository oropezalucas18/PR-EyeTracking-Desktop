<Page x:ConnectionId='1'
    x:Class="Flashback.Pages.StartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Flashback.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Helpers.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <!--BooleanToVisibilityConverter-->
        <converters:BooleanToVisibilityConverter x:ConnectionId='2' x:Name="BooleanToVisibilityConverter"/>
    </Page.Resources>

    <Grid 
        Background="{StaticResource BackgroundBrush}"
        Transitions="{StaticResource EntranceTransitionCollection}">
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <TextBlock
                Text="Eye Tracking"
                FontSize="42"
                FontFamily="ms-appx:///Assets/Fonts/Flashback.ttf#Signika"
                HorizontalAlignment="Center"
                Margin="24,0,24,8"/>
            <TextBlock
                Text="Generador de mapas de calor!"
                FontWeight="Light"
                HorizontalAlignment="Center"
                Margin="0,0,0,36"/>
            <Button x:ConnectionId='3'
                Content="Nuevo Mapa"
                                                                 
                HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Grid>
</Page>

